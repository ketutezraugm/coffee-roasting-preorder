openapi: 3.0.3
info:
  title: production service
  version: 1.0.0
  description: |
    Roasting, packing and shipping. Owns production batches, packing lines and shipments.
    Weights are integers in grams.
    Errors always use {"error": "<Code>", "message": "<text>"}.
servers:
  - url: http://localhost:3002

paths:
  /health:
    get:
      summary: Liveness check
      responses:
        "200":
          description: Service is up
          content:
            application/json:
              schema:
                type: object
                required: [status]
                properties:
                  status: { type: string, enum: [ok] }

  /production-batches:
    post:
      summary: Receive a closed batch from ordering
      description: |
        Also creates one shipment per line (state AwaitingPacking) from the recipient
        details. Sending the same batchId again returns 200 with the existing
        production batch and changes nothing.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/NewProductionBatch" }
      responses:
        "201":
          description: Queued
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ProductionBatchCreated" }
        "200":
          description: Already received, existing batch returned
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ProductionBatchCreated" }
        "422":
          description: InvalidProductionBatch
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
    get:
      summary: List production batches (the roaster's queue)
      description: Newest first. batchId is the id ordering sent, so a batch can be matched to its source.
      responses:
        "200":
          description: All production batches
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  required: [productionBatchId, batchId, beanName, roastLevel, status]
                  properties:
                    productionBatchId: { type: string }
                    batchId: { type: string }
                    beanName: { type: string }
                    roastLevel: { type: string, enum: [light, medium, dark] }
                    status: { type: string, enum: [Queued, Roasted, Packed] }

  /production-batches/{id}:
    parameters:
      - $ref: "#/components/parameters/id"
    get:
      summary: Status and lines of a production batch
      responses:
        "200":
          description: The production batch
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ProductionBatch" }
        "404": { $ref: "#/components/responses/NotFound" }

  /production-batches/{id}/roast:
    parameters:
      - $ref: "#/components/parameters/id"
    post:
      summary: Record the roast result
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [actualSellableGrams]
              properties:
                actualSellableGrams: { type: integer, minimum: 0 }
      responses:
        "200":
          description: Roasted
          content:
            application/json:
              schema:
                type: object
                required: [status]
                properties:
                  status: { type: string, enum: [Roasted] }
        "404": { $ref: "#/components/responses/NotFound" }
        "422":
          description: InvalidRoast
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
        "409":
          description: AlreadyRoasted
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /production-batches/{id}/packing-list:
    parameters:
      - $ref: "#/components/parameters/id"
    get:
      summary: What to grind and pack per order
      responses:
        "200":
          description: Packing list
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  required: [orderId, packSizeGrams, grind, quantity, packed]
                  properties:
                    orderId: { type: string }
                    packSizeGrams: { type: integer }
                    grind: { type: string, enum: [coarse, medium, fine] }
                    quantity: { type: integer }
                    packed: { type: boolean }
        "404": { $ref: "#/components/responses/NotFound" }

  /production-batches/{id}/lines/{orderId}/pack:
    parameters:
      - $ref: "#/components/parameters/id"
      - $ref: "#/components/parameters/orderId"
    post:
      summary: Mark one order packed
      description: |
        Also moves that order's shipment to ReadyToShip. The batch becomes Packed
        when all lines are packed.
      responses:
        "200":
          description: Packed
          content:
            application/json:
              schema:
                type: object
                required: [lineStatus, batchStatus]
                properties:
                  lineStatus: { type: string, enum: [Packed] }
                  batchStatus: { type: string, enum: [Roasted, Packed] }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: NotRoasted or AlreadyPacked
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
              examples:
                notRoasted: { value: { error: NotRoasted, message: "Record the roast first" } }
                packed: { value: { error: AlreadyPacked, message: "Line already packed" } }

  /shipments/{orderId}:
    parameters:
      - $ref: "#/components/parameters/orderId"
    get:
      summary: Shipment status
      responses:
        "200":
          description: The shipment
          content:
            application/json:
              schema:
                type: object
                required: [status]
                properties:
                  status: { $ref: "#/components/schemas/ShipmentStatus" }
                  trackingNumber: { type: string }
        "404": { $ref: "#/components/responses/NotFound" }

  /shipments/{orderId}/ship:
    parameters:
      - $ref: "#/components/parameters/orderId"
    post:
      summary: Roaster ships the order
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [trackingNumber]
              properties:
                trackingNumber: { type: string, minLength: 1 }
      responses:
        "200":
          description: Shipped
          content:
            application/json:
              schema:
                type: object
                required: [status]
                properties:
                  status: { type: string, enum: [Shipped] }
        "404": { $ref: "#/components/responses/NotFound" }
        "422":
          description: InvalidShipment
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
        "409":
          description: NotReady
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /shipments/{orderId}/confirm-receipt:
    parameters:
      - $ref: "#/components/parameters/orderId"
    post:
      summary: Buyer confirms receipt
      description: |
        Marks the shipment Delivered, then tells ordering to complete the order.
        If ordering is down the shipment stays Delivered and 502 is returned;
        calling again re-sends the notification (ordering's complete is idempotent).
      responses:
        "200":
          description: Delivered and ordering notified
          content:
            application/json:
              schema:
                type: object
                required: [status]
                properties:
                  status: { type: string, enum: [Delivered] }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: NotShipped
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
        "502":
          description: DownstreamUnavailable
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

components:
  parameters:
    id:
      name: id
      in: path
      required: true
      schema: { type: string }
    orderId:
      name: orderId
      in: path
      required: true
      schema: { type: string }

  responses:
    NotFound:
      description: Not found
      content:
        application/json:
          schema: { $ref: "#/components/schemas/Error" }

  schemas:
    Error:
      type: object
      required: [error, message]
      properties:
        error: { type: string }
        message: { type: string }

    ShipmentStatus:
      type: string
      enum: [AwaitingPacking, ReadyToShip, Shipped, Delivered]

    Line:
      type: object
      required: [orderId, packSizeGrams, grind, quantity]
      properties:
        orderId: { type: string }
        packSizeGrams: { type: integer, enum: [100, 250, 500, 1000] }
        grind: { type: string, enum: [coarse, medium, fine] }
        quantity: { type: integer, minimum: 1 }

    NewLine:
      allOf:
        - $ref: "#/components/schemas/Line"
        - type: object
          required: [recipientName, contact, address]
          properties:
            recipientName: { type: string }
            contact: { type: string }
            address: { type: string }

    NewProductionBatch:
      type: object
      required: [batchId, beanName, roastLevel, lines]
      properties:
        batchId: { type: string }
        beanName: { type: string }
        roastLevel: { type: string, enum: [light, medium, dark] }
        lines:
          type: array
          minItems: 1
          items: { $ref: "#/components/schemas/NewLine" }

    ProductionBatchCreated:
      type: object
      required: [productionBatchId, status]
      properties:
        productionBatchId: { type: string }
        status: { type: string, enum: [Queued, Roasted, Packed] }

    ProductionBatch:
      type: object
      required: [productionBatchId, status, lines]
      properties:
        productionBatchId: { type: string }
        status: { type: string, enum: [Queued, Roasted, Packed] }
        actualSellableGrams: { type: integer }
        lines:
          type: array
          items:
            allOf:
              - $ref: "#/components/schemas/Line"
              - type: object
                required: [packed]
                properties:
                  packed: { type: boolean }
